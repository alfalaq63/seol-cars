{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/next",
      "config": {
        "buildCommand": "npm run vercel-build"
      }
    }
  ],
  "env": {
    "PRISMA_SKIP_POSTINSTALL_GENERATE": "true"
  }
}
